# マージ前リファクタリング計画

**目的:** 振る舞いを一切変えずに、`feature/writing` で増えた重複と肥大を整理してから `main` へマージする。

**範囲外:** `docs/js/` と `app/ui/js/` の統合(公開サイトとローカルアプリの分離は意図的)。
ブリッジ層 (`store.native.js` / `essays.native.js`) と一覧描画 (`app.js` / `writing-list.js`) の
共通化。扱うデータが違い、いま抽象を1つ増やすと読みにくくなる。

---

## Task 1: JSONL の追記処理を共通化する

`main.swift:106-117` (StoreHandler) と `main.swift:174-185` (EssaysHandler) が逐語的に同じ。
`JSONLinesLog.swift` に値型 `JSONLinesFile` を置き、両方がそれを持つ形にする。

- Create: なし (`app/src/JSONLinesLog.swift` を拡張)
- Modify: `app/src/JSONLinesLog.swift`, `app/tests/test_jsonlines_log.swift`
- 追記処理はこれまでハンドラ内にあり単体テストが無かった。共通化と同時にテストを付ける。

- [ ] `JSONLinesFile` の失敗するテストを書く(新規作成 / 既存への追記 / 壊れた行の無視)
- [ ] テストが落ちることを確認
- [ ] `JSONLinesFile` を実装
- [ ] テストが通ることを確認
- [ ] コミット

## Task 2: main.swift (371行) を責務ごとに分割する

- Create: `app/src/ContentSchemeHandler.swift`, `app/src/LogHandlers.swift`, `app/src/GradeHandler.swift`
- Modify: `app/src/main.swift` (起動処理のみ残す), `app/build.sh`

- [ ] 3ファイルへ移動し、`main.swift` を起動処理だけにする
- [ ] `build.sh` のコンパイル対象に追加
- [ ] `sh app/build.sh` が通ることを確認
- [ ] `sh app/tests/run.sh` が通ることを確認
- [ ] コミット

## Task 3: HTML エスケープ関数を1つにする

`docs/js/textmatch.js` の `escapeHtml` と `app/ui/js/escape.js` の `escapeText` が同じ処理。
`escapeHtml` は文字列照合の責務ではないので `docs/js/escape.js` に切り出し、両方がそれを使う。

- Create: `docs/js/escape.js`, `tests/js/escape.test.js`
- Delete: `app/ui/js/escape.js`
- Modify: `docs/js/textmatch.js`, `tests/js/textmatch.test.js`,
  `app/ui/js/writing-list.js`, `app/ui/js/writing-editor.js`,
  HTML 6ファイル (`docs/index.html`, `docs/reader.html`, `app/ui/index.html`,
  `app/ui/reader.html`, `app/ui/writing.html`, `app/ui/writing-editor.html`)

- [ ] `docs/js/escape.js` と そのテストを作る
- [ ] `textmatch.js` から `escapeHtml` を外し、テストを更新
- [ ] HTML 6ファイルに `escape.js` を読み込ませる(`textmatch.js` より前)
- [ ] `escapeText` の呼び出しを `escapeHtml` に置き換え、`app/ui/js/escape.js` を削除
- [ ] JS テストが通ることを確認
- [ ] コミット

## 検証

全タスク完了後:

- `sh app/tests/run.sh` (Swift)
- `node --test "tests/js/**/*.test.js"` (glob 必須)
- `python3 -m unittest discover -s tests/python`
- `sh app/build.sh` 後にアプリを起動し、リーディング一覧・読解画面・
  ライティング一覧・エディタの4画面が壊れていないことを目視
- 公開サイト側 (`docs/index.html`, `docs/reader.html`) をブラウザで開いて確認
